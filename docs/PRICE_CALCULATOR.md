# Калькулятор ціни для жалюзі та штор

## Огляд

Калькулятор дозволяє клієнтам вводити власні розміри (ширина × висота) та отримувати точну ціну без необхідності створювати сотні варіацій товару.

## Як це працює

### Формула розрахунку ціни

```
ціна = (ширина_м × висота_м) × ціна_за_кв_м
```

Де:
- `ширина_м` = ширина в см / 100
- `висота_м` = висота в см / 100
- `ціна_за_кв_м` = поле `price_per_sqm` в Directus

Мінімальна ціна — базова ціна товару (поле `price`).

---

## Налаштування в Directus

### Нові поля для колекції `products`

Додайте наступні поля в колекцію `products`:

| Поле | Тип | Опис |
|------|-----|------|
| `price_per_sqm` | Decimal / Float | Ціна за квадратний метр (грн/м²) |
| `min_width` | Integer | Мінімальна ширина (см) |
| `max_width` | Integer | Максимальна ширина (см) |
| `min_height` | Integer | Мінімальна висота (см) |
| `max_height` | Integer | Максимальна висота (см) |
| `fixed_height` | Integer | Фіксована висота (см) — для римських штор |

### Приклади налаштувань

#### 🪟 Римські штори (фіксована висота 170 см)

```json
{
  "name": "Римська штора Elegance",
  "price": 1500,
  "price_per_sqm": 8824,
  "min_width": 50,
  "max_width": 200,
  "fixed_height": 170
}
```

**Пояснення:**
- `fixed_height: 170` — висота зафіксована, користувач вибирає тільки ширину
- `price_per_sqm: 8824` — розраховано так: `1500 / (1.0 × 1.7) ≈ 8824 грн/м²`
- Мінімальна ціна: 1500 грн (для розміру 100×170 см)

**Приклади цін:**
| Ширина | Площа | Ціна |
|--------|-------|------|
| 80 см | 0.8 × 1.7 = 1.36 м² | 12 000 грн |
| 100 см | 1.0 × 1.7 = 1.7 м² | 15 000 грн |
| 150 см | 1.5 × 1.7 = 2.55 м² | 22 500 грн |

#### 🪟 Горизонтальні жалюзі (довільні розміри)

```json
{
  "name": "Горизонтальні жалюзі Premium",
  "price": 800,
  "price_per_sqm": 5000,
  "min_width": 30,
  "max_width": 300,
  "min_height": 50,
  "max_height": 250
}
```

**Пояснення:**
- Немає `fixed_height` — користувач вибирає і ширину, і висоту
- Діапазон ширини: 30–300 см
- Діапазон висоти: 50–250 см

#### 🪟 Вертикальні жалюзі

```json
{
  "name": "Вертикальні жалюзі Office",
  "price": 1200,
  "price_per_sqm": 4000,
  "min_width": 100,
  "max_width": 400,
  "min_height": 150,
  "max_height": 300
}
```

---

## Логіка відображення

### Коли показується калькулятор?

Калькулятор автоматично з'являється якщо товар має:
- `price_per_sqm` — ціна за кв.м
- АБО `fixed_height` — фіксована висота

### Коли показується старий SizeSelector?

Якщо товар **не має** полів калькулятора, показується стандартний вибір розмірів з варіантів товару.

---

## Що зберігається в кошику

При додаванні товару з калькулятором в кошик зберігаються:

```typescript
{
  id: "product-id",
  name: "Римська штора Elegance",
  price: 15000,           // Розрахована ціна
  size: "100×170 см",     // Вибраний розмір
  customWidth: 100,       // Ширина (см)
  customHeight: 170,      // Висота (см)
  quantity: 1
}
```

---

## Переваги підходу

✅ **1 товар = 1 тип жалюзі** — замість 100500 варіацій

✅ **Точна ціна** — клієнт одразу бачить вартість для свого розміру

✅ **Чиста база** — не пухне від варіацій

✅ **Простий імпорт** — CSV з 6 полями замість тисяч рядків

✅ **SEO не страждає** — 1 сторінка на товар

✅ **UX** — слайдери та швидкі кнопки для вибору розміру

---

## Міграція існуючих товарів

Якщо у вас є товари з варіаціями типу "100x170", "120x170":

1. Залиште тільки один "базовий" товар
2. Додайте поля калькулятора:
   - `price_per_sqm` = стара_ціна / (базова_ширина × базова_висота)
   - `min_width`, `max_width` — діапазон з ваших варіацій
   - `fixed_height` — якщо висота однакова для всіх варіацій
3. Видаліть дублікати товарів

---

## Файли калькулятора

```
client/src/
├── components/product/
│   ├── PriceCalculator.tsx      # Компонент калькулятора
│   ├── ProductPurchaseSection.tsx # Обгортка (калькулятор + кошик)
│   └── AddToCartButton.tsx      # Оновлений для підтримки калькулятора
├── types/
│   └── index.ts                 # Оновлені типи Product, CartItem
└── lib/directus/
    └── client.ts                # Маппінг нових полів
```
